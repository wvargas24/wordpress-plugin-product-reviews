<?php
/**
 * Core PHP Skills Test
 * 
 * Instructions:
 * - Implement each function based on the provided description.
 * - Do not use external libraries.
 */

/**
 * Task 1: Generate a Random List of Integers
 * 
 * Write a function `generateRandomList($size)` that returns an array 
 * of `$size` random integers between 1 and 100.
 */
function generateRandomList($size) {
    // Implement function here
}

/**
 * Task 2: Find the Longest Word in a Sentence
 * 
 * Write a function `longestWord($sentence)` that returns the longest 
 * word in the given `$sentence` string.
 */
function longestWord($sentence) {
    // Implement function here
}

/**
 * Task 3: Calculate Factorial Using Recursion
 * 
 * Write a function `factorial($n)` that calculates the factorial 
 * of `$n` using recursion.
 */
function factorial($n) {
    // Implement function here
}

// Example test cases
echo "Task 1: " . implode(", ", generateRandomList(5)) . "\n";
echo "Task 2: " . longestWord("The quick brown fox jumps over the lazy dog") . "\n";
echo "Task 3: " . factorial(5) . "\n";
